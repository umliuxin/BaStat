== README

BaStat

URL: http://www.lsjball.club/


* Installation

 ## Clone this repo
 ## Bundler
 `gem install bundle`
 `bundle install`
 ## Database Migration
 `rake db:migrate`
 ##

* Dependency

 1. Heroku CLI
 2. Ruby 2.2.5
 3. Rails 5.1.0
 4. Bundler
 5. PostGrasql


* Deployment

  `git push heroku master`

* Set up
1. Goto `/admin` to create one season and set it to `current_season`
